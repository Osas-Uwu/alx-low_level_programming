#include "main.h"
#include <string.h>

/**
 * _strlen - check the length of a string
 *
 * @: string value
 *
 * Return: value of length of string
 */
int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
